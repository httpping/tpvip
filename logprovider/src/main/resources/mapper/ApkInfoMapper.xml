<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tp.api.mapper.ApkInfoMapper">

    <!-- 开启二级缓存 -->
    <!--<cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>-->

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.tp.api.entity.ApkInfo">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="number" property="number" />
        <result column="file_name" property="fileName" />
        <result column="path" property="path" />
        <result column="size" property="size" />
        <result column="emails" property="emails" />
        <result column="changeLog" property="changeLog" />
        <result column="duration" property="duration" />
        <result column="update_time" property="updateTime" />
        <result column="created_time" property="createdTime" />
        <result column="status" property="status" />
        <result column="plist_url" property="plistUrl" />
        <result column="platform" property="platform" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, name, number, file_name, path, size, emails, changeLog AS changeLog, duration, update_time, created_time, status,platform,plist_url
    </sql>


    <select id="apkGroup"  resultMap="BaseResultMap">
        select a.* from apk_info a where number = (select max(number) from apk_info where name = a.name) order by a.update_time desc
    </select>
</mapper>
